<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ChepaMotos.Views.DashboardsView">

    <VerticalStackLayout Padding="24" Spacing="20">

        <Label Text="Dashboards"
               Style="{StaticResource PageHeading}" />

        <!-- Placeholder for Metabase WebView -->
        <Border Stroke="{StaticResource Border}"
                StrokeShape="RoundRectangle 8"
                StrokeThickness="1"
                BackgroundColor="{StaticResource Surface}"
                HeightRequest="500"
                Padding="0">
            <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="8">
                <Label Text="📊"
                       FontSize="48"
                       HorizontalTextAlignment="Center" />
                <Label Text="Metabase Dashboard"
                       FontFamily="IBMPlexSansSemiBold"
                       FontSize="16"
                       TextColor="{StaticResource TextSecondary}"
                       HorizontalTextAlignment="Center" />
                <Label Text="WebView se cargará aquí con los dashboards de Metabase"
                       FontFamily="IBMPlexSans"
                       FontSize="13"
                       TextColor="{StaticResource TextMuted}"
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </Border>

    </VerticalStackLayout>

</ContentView>
